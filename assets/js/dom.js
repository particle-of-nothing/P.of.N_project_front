// const categories = [{ name: '111', id: 1 }, { name: '222', id: 2 }, { name: '333', id: 3 }];

// categoriesMenuElement = document.querySelector('body');

// const categoriesElements = categories.map(c => {
//     const linkElement = document.createElement('a');

//     linkElement.href = 'http://localehost:8080/products/${c.id}';
//     linkElement.innerText = c.name;
//     linkElement.classList.add('category-${c.id}');

//     return linkElement;
// });

// categoriesElements.forEach(e =>{
//     categoriesMenuElement.appendChild(linkElement);
// });